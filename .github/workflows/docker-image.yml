name: Docker Image CI

on:
  push:
    branches: [ "main" ]

jobs:

  build-and-push:

    runs-on: ubuntu-latest

    steps:
    - name: checkout repo
    - uses: actions/checkout@v4

    - name: docker  login
    - uses: docker/login-action@v2
      with:
        username: ramesh0112 
        password: dckr_pat_eyN7QS2jF9Rz0TCF62Q5So_KwOw
      
    - name: push
    - uses: docker/build-push-action@v4
      with: 
        context: .
        push: true
        tags: test-image:$(date +%s)
